type Account implements Auditable {
  id: ID!
  name: String!
  slug: String!
  type: AccountType!
  ownerId: ID!
  owner: User!
  projects: [Project!]
  createdAt: Date!
  updatedAt: Date!
  deletedAt: Date
}

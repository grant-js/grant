mutation Login($input: LoginInput!) {
  login(input: $input) {
    accessToken
    refreshToken
    requiresEmailVerification
    verificationExpiry
    email
    accounts {
      id
      type
      owner {
        id
        name
        pictureUrl
        updatedAt
      }
    }
  }
}

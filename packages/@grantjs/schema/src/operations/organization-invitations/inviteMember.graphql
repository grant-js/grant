mutation InviteMember($input: InviteMemberInput!) {
  inviteMember(input: $input) {
    id
    organizationId
    email
    roleId
    status
    expiresAt
    invitedBy
    invitedAt
    createdAt
  }
}

mutation UpdateProject($id: ID!, $input: UpdateProjectInput!) {
  updateProject(id: $id, input: $input) {
    id
    name
    slug
    description
    pictureUrl
    primaryColor
    showHelpPanel
    createdAt
    updatedAt
  }
}

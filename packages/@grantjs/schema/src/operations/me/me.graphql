query Me {
  me {
    accounts {
      id
      type
      ownerId
      createdAt
      updatedAt
      owner {
        id
        name
        pictureUrl
        metadata
        roleCount
        permissionCount
        projectUserApiKeyCount
        tagCount
        createdAt
        updatedAt
      }
    }
    mfaVerified
    requiresEmailVerification
    verificationExpiry
    email
  }
}

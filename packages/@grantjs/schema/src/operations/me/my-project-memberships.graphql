query MyProjectMemberships {
  myProjectMemberships {
    projectId
    projectName
    displayName
    pictureUrl
    metadata
    role
    joinedAt
    organizationId
    organizationName
    accountId
  }
}

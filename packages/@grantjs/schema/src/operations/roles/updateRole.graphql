mutation UpdateRole($id: ID!, $input: UpdateRoleInput!) {
  updateRole(id: $id, input: $input) {
    id
    name
    description
    metadata
    createdAt
    updatedAt
    tags {
      id
      name
      color
      isPrimary
    }
  }
}

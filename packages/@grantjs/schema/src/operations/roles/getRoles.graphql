query GetRoles(
  $scope: Scope!
  $page: Int
  $limit: Int
  $sort: RoleSortInput
  $search: String
  $ids: [ID!]
  $tagIds: [ID!]
) {
  roles(
    scope: $scope
    page: $page
    limit: $limit
    sort: $sort
    search: $search
    ids: $ids
    tagIds: $tagIds
  ) {
    roles {
      id
      name
      description
      metadata
      createdAt
      updatedAt
      # Group ids/names only — no nested group.tags (unused by attachment UIs).
      groups {
        id
        name
      }
      rolePermissions {
        id
        permissionId
      }
      tags {
        id
        name
        color
        isPrimary
      }
    }
    totalCount
    hasNextPage
  }
}

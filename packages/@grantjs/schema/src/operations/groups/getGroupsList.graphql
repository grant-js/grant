query GetGroupsList(
  $scope: Scope!
  $page: Int
  $limit: Int
  $sort: GroupSortInput
  $search: String
  $ids: [ID!]
  $tagIds: [ID!]
) {
  groups(
    scope: $scope
    page: $page
    limit: $limit
    sort: $sort
    search: $search
    ids: $ids
    tagIds: $tagIds
  ) {
    groups {
      id
      name
      description
      metadata
      createdAt
      updatedAt
      primaryTag {
        id
        name
        color
        isPrimary
      }
      tags {
        id
        name
        color
        isPrimary
      }
      permissionCount
      tagCount
    }
    totalCount
    hasNextPage
  }
}

extend type Mutation {
  """
  Update the authenticated user's project membership profile (display name / picture).
  """
  updateMyProjectMembership(input: UpdateMyProjectMembershipInput!): MyProjectMembership!
}

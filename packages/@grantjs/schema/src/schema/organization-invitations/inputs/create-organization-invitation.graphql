input CreateOrganizationInvitationInput {
  organizationId: ID!
  email: String!
  roleId: ID!
  token: String!
  expiresAt: Date!
  invitedBy: ID!
  invitedAt: Date
  status: OrganizationInvitationStatus
}

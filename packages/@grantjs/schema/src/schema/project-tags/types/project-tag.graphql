type ProjectTag implements Auditable {
  id: ID!
  projectId: ID!
  tagId: ID!
  isPrimary: Boolean!
  project(organizationId: ID!): Project
  tag: Tag
  createdAt: Date!
  updatedAt: Date!
  deletedAt: Date
}

type Group implements Auditable {
  id: ID!
  name: String!
  description: String
  metadata: JSON!
  permissions: [Permission!]
  tags: [Tag!]
  primaryTag: Tag
  permissionCount: Int!
  tagCount: Int!
  createdAt: Date!
  updatedAt: Date!
  deletedAt: Date
}

type Permission implements Auditable {
  id: ID!
  name: String!
  description: String
  action: String!
  resourceId: ID
  resource: Resource
  condition: JSON
  metadata: JSON!
  tags: [Tag!]
  primaryTag: Tag
  tagCount: Int!
  createdAt: Date!
  updatedAt: Date!
  deletedAt: Date
}

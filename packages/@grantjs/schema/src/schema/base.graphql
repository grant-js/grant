scalar Date
scalar JSON

interface Searchable {
  page: Int
  limit: Int
  search: String
  ids: [ID!]
}

interface Auditable {
  id: ID!
  createdAt: Date!
  updatedAt: Date!
  deletedAt: Date
}

interface PaginatedResults {
  totalCount: Int!
  hasNextPage: Boolean!
}

enum Tenant {
  system
  account
  organization
  projectUser
  accountProject
  organizationProject
  accountProjectUser
  organizationProjectUser
}

enum SortOrder {
  ASC
  DESC
}

input Scope {
  tenant: Tenant!
  id: ID!
}

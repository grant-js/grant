{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "lib": ["esnext"],
    "types": ["node"]
  },
  "include": [
    "lib/**/*",
    "bin/**/*",
    "scripts/**/*.ts",
    "vitest.config.ts",
    "vitest.global-setup.ts"
  ],
  "exclude": ["cdk.out", "cdk.snapshot", "node_modules"]
}

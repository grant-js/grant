/** Job id for the on-demand event relay (enqueueOnly fast path). */
export const EVENT_RELAY_JOB_ID = 'event-relay';

/** Job id for the scheduled event relay sweep (durability backstop). */
export const EVENT_RELAY_SWEEP_JOB_ID = 'event-relay-sweep';

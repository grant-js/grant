// Endpoint definitions are registered with the shared registry by
// config.openapi.ts, which imports each `*.openapi.ts` module directly.
export { generateOpenApiDocument, getOpenApiDocument } from './config.openapi';

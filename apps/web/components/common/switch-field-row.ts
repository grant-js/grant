import { cn } from '@/lib/utils';

/** Matches Input / SelectTrigger height and padding for bordered switch rows. */
export const SWITCH_FIELD_ROW_CLASS = cn(
  'flex h-9 items-center justify-between gap-4 rounded-md border border-input bg-transparent px-3 shadow-xs'
);

export const SWITCH_FIELD_ROW_LABEL_CLASS = 'text-sm font-normal leading-none';

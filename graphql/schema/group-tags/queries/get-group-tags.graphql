extend type Query {
  """
  Retrieves group-tag relationships for a specific group.
  """
  groupTags(
    """
    Scope of the group-tag relationships.
    """
    scope: Scope!
    """
    ID of the group to get tags for.
    """
    groupId: ID!
  ): [GroupTag!]!
}

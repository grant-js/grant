extend type Query {
  """
  Retrieves user-tag relationships for a specific user.
  """
  userTags(
    """
    Scope of the user-tag relationships.
    """
    scope: Scope!
    """
    ID of the user to get tags for.
    """
    userId: ID!
  ): [UserTag!]!
}

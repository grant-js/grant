# Grant docs – env used by VitePress at build/dev (integration guide, API config default).
# Copy to docs/.env for local dev. When building the docs Docker image, pass these as build args.
VITE_API_URL=http://localhost:4000
VITE_APP_URL=http://localhost:3000
VITE_EXAMPLE_APP_URL=http://localhost:3004
